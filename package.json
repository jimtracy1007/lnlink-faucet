{
  "name": "lnd-faucet",
  "version": "1.0.0",
  "description": "LND and RGB faucet with Nostr integration",
  "main": "index.js",
  "scripts": {
    "start:dev": "yarn prisma:generate && yarn prisma:migrate && cross-env ENV=dev nodemon index.js",
    "start": "yarn prisma:generate && yarn prisma:deploy && node index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "cors": "^2.8.5",
    "dayjs": "^1.11.10",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nostr-tools": "1.17.0",
    "pino": "^8.16.2",
    "pino-pretty": "^10.2.3",
    "pino-roll": "^4.0.0",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "nodemon": "^3.0.2",
    "prisma": "^5.7.0"
  }
}
